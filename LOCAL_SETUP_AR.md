# الفولدر المحلي المتصل بالمشروع

هذا الفولدر مربوط بالمستودع:
https://github.com/abdelrahmanmahmoudmohamed1-afk/operation-system

الفرع المحلي `main` يتابع `origin/main`، وتاريخ المستودع موجود داخل `.git`.
احتفظ بالفولدر كله بما فيه `.git`؛ تنزيل ZIP جديد لا يحتفظ بالربط.

## استخدام GitHub Desktop

1. اختر File → Add local repository.
2. اختر هذا الفولدر نفسه، الذي يحتوي `package.json` و`index.html`، ثم Add repository.
3. عدّل الملفات داخل هذا الفولدر.
4. راجع التغييرات ثم Commit to main وبعدها Push origin.

إذا كان المستودع القديم ظاهرًا بمسار محذوف ويعرض Locate، اختر هذا الفولدر بدلًا منه.
وصول تغييرات الملفات إلى الموقع يحتاج رفعها إلى GitHub ثم نجاح النشر من مزود الاستضافة؛ مجرد الحفظ محليًا لا ينشرها.

## الإصلاحات الموجودة محليًا

- تأخير الخروج من الحساب إلى ما بعد فشل محاولة تجديد الجلسة.
- توحيد طلب تجديد الجلسة عند تزامن الطلبات وإضافة مهلة له.
- منع تجديد جلسة أُغلقت أثناء انتظار الرد.
- منع إعادة المحاولة التلقائية لعمليات الحفظ والحذف والعمليات ذات الآثار الجانبية التي كانت ناقصة من القائمة.
- فصل محاولات تسجيل الدخول وعمليات الكتابة المتزامنة.
- تطبيق فلتر المشروع قبل قراءة البيانات المخزنة دون اتصال.
- إزالة رمز التجديد السابق عند تسجيل دخول جديد لا يرجع رمز تجديد.
- تجاهل مكتبات التشغيل وإعدادات Vercel المحلية في Git.

التحقق: `node --test scripts/api-regression.test.mjs` و`node scripts/release-audit.mjs`.
الاختبارات تحاكي ردود الخادم ولا تتصل بقاعدة البيانات الحقيقية. التشغيل الكامل وتأكيد النشر يحتاجان إعدادات الخدمات والحسابات المرتبطة.
الإصلاحات متروكة كتغييرات محلية قابلة للمراجعة، ولم تُرفع إلى GitHub.
