# سجل مراجعة Operation System

## النسخة المُراجعة

### إصلاحات تشغيلية
- إصلاح تغيير كلمة المرور؛ كانت شاشة الإعدادات تستدعي دالة غير موجودة في `AuthService`.
- تفعيل retry فعلي لطلبات الـ API بدل وجود الإعداد بدون استخدام.
- معالجة ردود Google Apps Script التي ترجع HTTP 200 لكن بداخلها `ok: false` أو `status: 401`.
- إعادة المستخدم لشاشة الدخول تلقائيًا عند انتهاء الجلسة.
- تحسين التعامل مع timeout، انقطاع الإنترنت، والرد غير الصالح من السيرفر.
- إضافة تنبيه واضح عند فقد الاتصال وعودة الإنترنت.

### أمان
- نقل التوكن من `localStorage` إلى `sessionStorage` حتى لا يظل محفوظًا بعد إغلاق المتصفح.
- حماية بيانات الـ bootstrap والقوائم في الباك إند بالتوكن بدل إتاحتها بدون تسجيل دخول.
- إضافة حد لمحاولات تسجيل الدخول: 5 محاولات ثم إيقاف مؤقت 15 دقيقة.
- منع حقن HTML في نافذة التفاصيل ورسالة فشل تشغيل النظام.
- إضافة `.gitignore` وتنبيه واضح بعدم رفع Backend وأرقام الشيتات على Repository عام.

### GitHub Pages وتنظيم المشروع
- حذف نسخة `Frontend` المكررة لتجنب تعديل نسخة وتشغيل نسخة أخرى.
- حذف مجلد `.git` من ملف التسليم.
- إضافة `.nojekyll` لضمان نشر الملفات كما هي.
- إضافة `404.html` لإرجاع الروابط غير الصحيحة إلى التطبيق.
- إضافة ملفات توثيق وتنبيه أمني.

### تحقق فني
- فحص Syntax لكل ملفات JavaScript بنجاح.
- فحص جميع مسارات Imports المحلية وعدم وجود ملفات مفقودة.
- التأكد أن `index.html` موجود في Root وجاهز لـ GitHub Pages.
