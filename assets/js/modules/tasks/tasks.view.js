import { escapeHtml } from '../../utils/helpers.js';
function group(rows,status){return rows.filter(x=>(x.status||'Open')===status)}
export function layout(){return `<div class="page-header"><div><div class="eyebrow">Follow-up workspace</div><h1>Tasks & Follow-ups</h1><p>Keep every client, lead and unit action accountable.</p></div><div class="page-header-actions"><button class="btn btn-primary" id="task-add">+ New Task</button></div></div><div class="filter-bar"><div class="filter-field"><label>Search</label><input id="task-search" placeholder="Task, client, unit, lead..."></div><div class="filter-field"><label>Priority</label><select id="task-priority"><option>ALL</option><option>High</option><option>Medium</option><option>Low</option></select></div><div class="filter-field"><label>Status</label><select id="task-status"><option>ALL</option><option>Open</option><option>In Progress</option><option>Done</option></select></div></div><div id="task-summary" class="kpi-grid compact-kpis"></div><div class="task-board" id="task-board"></div><div id="task-modal-root"></div>`}
export function summary(rows){const open=rows.filter(x=>x.status!=='Done').length,done=rows.filter(x=>x.status==='Done').length,over=rows.filter(x=>x.status!=='Done'&&x.dueDate&&new Date(x.dueDate)<new Date()).length,high=rows.filter(x=>x.status!=='Done'&&x.priority==='High').length;return [['Open',open],['Completed',done],['Overdue',over],['High Priority',high]].map(([a,b])=>`<div class="kpi-card"><div class="kpi-title">${a}</div><div class="kpi-value">${b}</div></div>`).join('')}
export function board(rows){const groups=['Open','In Progress','Done'];return groups.map(s=>`<section class="task-column"><div class="task-column-head"><strong>${s}</strong><span>${group(rows,s).length}</span></div><div class="task-column-body">${group(rows,s).map(card).join('')||`<div class="empty-state compact"><span>No ${s.toLowerCase()} tasks</span></div>`}</div></section>`).join('')}
function card(x){return `<article class="task-card" data-id="${escapeHtml(x.id)}"><div class="task-card-top"><span class="priority-pill p-${String(x.priority||'Medium').toLowerCase()}">${escapeHtml(x.priority||'Medium')}</span><button class="icon-btn task-delete" title="Delete">×</button></div><h3>${escapeHtml(x.title||'Untitled')}</h3><p>${escapeHtml(x.notes||'')}</p><div class="task-meta"><span>${escapeHtml(x.linkedType||'General')}</span><span>${escapeHtml(x.linkedRef||'')}</span></div><div class="task-card-foot"><small>${escapeHtml(x.dueDate||'No due date')}</small><button class="btn btn-outline btn-sm task-toggle">${x.status==='Done'?'Reopen':'Advance'}</button></div></article>`}
export function modal(){return `<div class="modal-backdrop" id="task-modal"><div class="modal-box enterprise-form-modal"><div class="modal-heading"><div><h2>New Task</h2><p class="modal-subtitle">Create a follow-up linked to any record.</p></div><button class="modal-close" id="task-modal-close">×</button></div><form id="task-form"><div class="form-grid"><div class="field-full"><label>Task title</label><input name="title" required></div><div><label>Priority</label><select name="priority"><option>High</option><option selected>Medium</option><option>Low</option></select></div><div><label>Due date</label><input type="date" name="dueDate"></div><div><label>Link type</label><select name="linkedType"><option>Client</option><option>Lead</option><option>Unit</option><option>Contract</option><option>General</option></select></div><div><label>Reference</label><input name="linkedRef" placeholder="Unit code, client, lead..."></div><div class="field-full"><label>Notes</label><textarea name="notes" rows="4"></textarea></div></div><div class="form-actions"><button type="button" class="btn btn-outline" id="task-cancel">Cancel</button><button class="btn btn-primary" type="submit">Save Task</button></div></form></div></div>`}
