# متطلبات اختبار الإنتاج — Operation System

## تم محليًا
- بناء الواجهة، فحص الملفات، اختبارات مطابقة البيانات، واختبارات ترقيم EOI.
- الحفاظ على الملفات وIDs الأصلية، واستكمال البحث والطباعة الموجودة.

## مطلوب قبل التشغيل الحقيقي
1. خذ نسخة احتياطية من Google EOI Sheet وقاعدة Supabase.
2. أضف عمودًا جديدًا بعنوان `EOI Request ID` إلى صف عناوين تبويب الـEOI الحالي، **دون تغيير أو مسح أي عمود موجود**. النظام يرفض الحفظ إن كان العمود غير موجود، لحماية إعادة المحاولة من التكرار.
3. افحص الأرقام المكررة القديمة في Supabase قبل تطبيق `migrations/007_eoi_numbering_safe.sql`، ثم نفّذ الهجرة.
4. اضبط `GOOGLE_SERVICE_ACCOUNT_EMAIL` و`GOOGLE_PRIVATE_KEY` و`POSTGRES_URL` أو `DATABASE_URL`، وتأكد من صلاحية تعديل الشيت.
5. جرّب عمليتين حفظ متزامنتين، إعادة محاولة نفس الطلب بعد مهلة شبكة، رقم EOI قديم، وتصدير PDF من نافذة الطباعة.
6. راجع صلاحيات المستخدمين وتقارير كل قسم على الحسابات الفعلية، ثم انشر على Vercel.

**حد تقني:** لا توجد معاملة ذرية مشتركة بين Google Sheets وPostgres. استخدم نفس EOI Request ID عند إعادة محاولة حفظ غير مؤكدة؛ وإذا لم يمكن التحقق من النتيجة، طابق السجلين قبل إنشاء طلب جديد. الكتابات الخارجية المباشرة للشيت خارج قفل التطبيق قد تتسابق مع النظام؛ اجعل إنشاء EOIs الجديدة عبر النظام فقط.
